# ArtiChat 0.1.3 Milestone 4 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Execution constraint:** Execute inline; do not use subagents, push GitHub, or touch production.

**Goal:** Add registration controls, SMTP configuration, customizable mail, verification-code login, password reset, and email verification for sensitive account actions.

**Architecture:** Store non-secret switches in Config, encrypt SMTP credentials with a Fernet key derived from `WEBUI_SECRET_KEY`, and keep verification challenges/reset tokens in dedicated additive SQLAlchemy tables. A single mail service renders whitelisted variables into subject/Markdown templates, records deliveries, and sends through stdlib SMTP in a worker thread. Auth routes call the verification service; OAuth and LDAP remain unchanged.

**Tech Stack:** FastAPI, Pydantic, SQLAlchemy async, Alembic, cryptography/Fernet, hashlib/secrets, smtplib/email.message, Markdown, pytest, SvelteKit.

---

## Task 1: Registration Rules And Security Tables

- [x] Add tests for normalized domain allowlists, optional subdomains, empty allowlist, existing/admin bypass, code expiry, resend cooldown, hourly email/IP limits, five attempts, consume-on-success, and reset-token single use.
- [x] Create migration `b5c6d7e8f9a0_add_email_security_tables.py` after `a4b5c6d7e8f9`.
- [x] Add `email_challenge`, `password_reset_token`, `email_template`, and `email_delivery` tables with hashes only for codes/tokens.
- [x] Add pure services under `backend/open_webui/utils/email_security.py`.
- [x] Commit: `feat: add email security foundations`.

## Task 2: SMTP Configuration, Templates, And Delivery

- [x] Add tests for secret encryption/decryption, masked admin responses, TLS/SSL modes, connection-test stage errors, template variable allowlists, HTML/plain rendering, delivery recording, and retry.
- [x] Add Config keys for enabled, host, port, username, encrypted password, security mode, sender address/name, reply-to, public URL, and subscription notifications.
- [x] Seed default templates for registration code, login code, sensitive-action code, password reset, password-changed notice, email-changed notice, billing-address notice, subscription notice, and SMTP test.
- [x] Add admin APIs for settings, connection test, test email, template CRUD, delivery list, and retry.
- [x] Add independent Email settings and Templates pages in admin navigation.
- [x] Commit SMTP and customizable email delivery in bounded, recoverable commits.

Checkpoint: Task 2 is complete across SMTP secret encryption/masking, settings normalization, default templates, variable allowlists, HTML/plain rendering, connection stages, persistent delivery state, sensitive-variable redaction, retry, Config defaults, admin APIs, frontend bindings, and independent mobile-visible admin pages. Next: Task 3 registration verification and email-code login.

## Task 3: Registration Verification And Email Code Login

- [x] Add registration config API for domain allowlist, subdomain switch, registration verification, email-code login, and sensitive-action verification.
- [x] Add request/verify code APIs with generic anti-enumeration responses.
- [x] Apply domain rules before signup; initial first-admin creation bypasses verification.
- [x] When registration verification is enabled, require a consumed registration challenge before account creation and mark email verified.
- [x] Add email-code login as an alternate tab on the login page; keep password, OAuth, and LDAP behavior unchanged.
- [x] Commit: `feat: add verified registration and email login`.

Checkpoint: registration Config/API/UI, challenge lifecycle, generic dispatch, one-use verification tickets, verified signup, and email-code sessions are complete. Failed mail invalidates its challenge, successful verification consumes it, and account actions claim each ticket once. Next: Task 4 password reset and sensitive-action verification.

## Task 4: Password Reset And Sensitive Verification

- Add forgot-password endpoint returning a generic response, one-time 30-minute reset links, reset page, and password-reset security notice.
- Add session-bound 10-minute sensitive verification grants.
- Require a valid grant for password, billing-address, and login-email changes when enabled.
- Email change verifies current email first and new email second.
- Invalidate old sessions after reset using existing session-disconnect mechanisms.
- Commit: `feat: protect password and billing changes with email`.

Checkpoint: the generic forgot-password endpoint, HMAC-only 30-minute reset tokens, newer-link invalidation, one-time consumption, reset page, login-page entry, password-changed notice, and persistent old-session invalidation are complete. Next: session-bound sensitive-action grants.

Checkpoint: session-bound, action-bound, one-use grants now protect password and payment-information changes and have inline user settings UI. Next: current-email then new-email verification for login-email changes.

## Task 5: Notifications, UI Completion, And Regression

- Send post-commit subscription-change notices without rolling back subscription success on mail failure.
- Add default admin pages for registration rules, SMTP, templates, and delivery history with test actions.
- Add actionable frontend states for cooldown, expiry, retry, and configuration errors.
- Run migration rehearsal, backend suites, guards, Vitest, Vite build, Docker build, and record the existing global Svelte-check baseline separately.
- Update progress and commit: `docs: record ArtiChat 0.1.3 milestone 4`.

## Security Invariants

- Codes are six digits, valid ten minutes, resend cooldown sixty seconds, max five sends per email/hour, max five attempts, and consumed immediately on success.
- Reset links are valid thirty minutes and single use.
- Public request responses never reveal whether an email exists.
- SMTP passwords, codes, and reset tokens never appear in normal logs or API responses.
- Existing users remain able to password-login after upgrade; OAuth/LDAP are not routed through local email verification.
- Initial admin bootstrapping cannot be locked out by missing SMTP configuration.
- Business email failure never rolls back a committed subscription change.
