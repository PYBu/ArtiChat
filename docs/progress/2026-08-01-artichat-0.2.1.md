# ArtiChat 0.2.1 Release Qualification Plan

Last updated: 2026-08-02 01:25 (Asia/Hong_Kong)

Status: private candidate qualified through copied-production-volume upgrade and rollback rehearsal. Production port 3000, commit, push, tag, publication, and release remain deliberately unperformed.

## Version Decision

- The next public ArtiChat release is `0.2.1`. The accumulated Open WebUI 0.11 foundation work and subsequent Operations, billing, upgrade, and About revisions are too broad to publish as the previously planned `0.2.0`.
- `0.2.0` remains an unpublished implementation-history label and will not be published. The supported direct upgrade under qualification is `0.1.7 -> 0.2.1`; users must not need an intermediate `0.2.0` installation.
- Work remains in `C:\Users\admin\Desktop\Pro\ArtiChat\.worktrees\artichat-0.2.0` on branch `artichat-0.2.0` until the large uncommitted forward-port tree is reviewed. Do not rename or recreate the worktree during qualification merely for naming consistency.
- Code and package version sources are aligned to `0.2.1`. Historical 0.2.0 references remain only where they describe implementation history, dependencies, or old release artifacts.
- A local upstream tag named `v0.2.1` already exists at `cfc78dedf0024ecb97a5090825af2698524c9d71` (`Merge pull request #2736 from open-webui/dev`, 2024-06-02). `origin` had no `v0.2.1` tag when checked. Resolve the local collision deliberately during authorized release work; never force or overwrite a remote tag.

## Explicitly Approved Release Documentation Scope

- The user explicitly approved updating `README.md` and images under `artivis-ass/` for the 0.2.1 GitHub release. This is a release-preparation exception to their normal user-owned status.
- Do not edit those paths before the final 0.2.1 UI and runtime are accepted. Capture sanitized screenshots from the final candidate only; do not expose user data, email addresses, secrets, internal URLs, tokens, or private operational details.
- Update README version/upgrade guidance and image references together so the published documentation cannot describe a different UI or upgrade path from the release artifact.

## Required Execution Order

### 1. Switch And Qualify The Private Candidate

- Health-gate replacement of port 3003 from overlay `artichat:0.2.0-about-20260731` to the complete image `artichat:0.2.0-about-20260801-full-retry-cache`, preserving the private copied volume, runtime configuration, persistent secret-key path, and the old candidate as a rollback unit.
- Verify `/ready`, `/health`, `/api/version`, migrations, root adapter-static layout, login, all changed Operations pages, Usage Ledger, user About, desktop/mobile rendering, and absence of console errors or HTTP 5xx responses.
- Run the dependency-complete backend suite inside that exact full image and a clean empty-volume smoke. A static bundle check alone is insufficient.

### 2. Align And Build The 0.2.1 Release Candidate

- After the existing full image passes private 3003 qualification, change semantic version sources from 0.2.0 to 0.2.1 and audit product-facing hard-coded `0.2.0` strings, tests, update metadata, deployment examples, release checks, changelog, and release notes. Preserve historical references only where they describe an actual past implementation or migration fact.
- Build a clean full 0.2.1 release-candidate image from the version-aligned tree. Run release-version, branding, update, migration, backend, frontend, static image, and empty-volume gates before using it for upgrade qualification.
- Do not retag the earlier 0.2.0-labeled qualification image. Every subsequent upgrade, port-3000, and publication check must identify the exact 0.2.1 candidate digest.

### 3. Prove The Direct 0.1.7 To 0.2.1 Upgrade

- Do not use the live `artichat_data` volume for the first upgrade attempt. Create and checksum a fresh 0.1.7 archive, fingerprint the main SQLite database, Chroma database, and retained files, then restore or clone them into disposable private volumes.
- Exercise both an empty 0.1.7 fixture and a current production-data copy with the exact 0.2.1 release candidate. Verify migration head/readiness, schema idempotence across restart, all durable row/file fingerprints, authentication, chats/messages/files/knowledge, model configuration, subscriptions, Plan/recharge balances, usage and ledgers, redemption/gifts, announcements/views, email configuration encryption, application key persistence, and file permissions.
- Confirm direct skip-version behavior: a published 0.1.7 deployment must upgrade straight to 0.2.1 without requiring 0.2.0.
- Rehearse rollback by restoring the pre-upgrade archive to a fresh volume and starting the retained 0.1.7 image. Compare fingerprints again. Never attempt to run 0.1.7 against a data volume already migrated in place to 0.2.1.

### 4. Final Local Port-3000 Upgrade Gate

- Only after copied-volume qualification passes, present the evidence and obtain the final go/no-go for the real port-3000 replacement.
- For an approved replacement, stop writers, create and validate a new production archive, retain the 0.1.7 container/image plus matching archive, and health-gate 3000 onto the exact tested 0.2.1 image digest without changing the named volume or runtime configuration unexpectedly.
- Run authenticated desktop/mobile acceptance and billing/data invariants on 3000. Restore the complete 0.1.7 rollback unit immediately if readiness or acceptance fails. Any code fix after this point invalidates the candidate and requires rebuilding and rerunning the affected gates.

### 5. Documentation, Commit Review, And GitHub Publication

- After final UI acceptance, update `README.md` and `artivis-ass/` screenshots for 0.2.1, then verify links and image rendering locally.
- Review the entire large dirty tree, exclude temporary logs/build outputs/private artifacts, and produce deliberate commits plus 0.2.1 release notes. Confirm the exact commit, tag, image digest, migration head, tests, backup, and rollback unit before publication.
- Decide and document final GHCR tags, then push the approved commits, create the ArtiChat `v0.2.1` tag and GitHub release, and publish the exact tested container image digest only after every prior gate is green. If any gate fails, stop before publication and retain the last healthy runtime.

## Next Resume Point

1. Restore this plan and `docs/progress/2026-07-28-artichat-0.2.0.md`; confirm the active worktree, HEAD, dirty tree, Docker inventory, and both health endpoints.
2. Switch only the private 3003 candidate to `artichat:0.2.0-about-20260801-full-retry-cache` with rollback retention, then run image-complete tests and authenticated browser acceptance.
3. Align version sources to 0.2.1, build and baseline-test the exact release-candidate image, then use that digest for the isolated `0.1.7 -> 0.2.1` copied-volume upgrade matrix and rollback rehearsal. Do not touch live `artichat_data` during this stage.
4. Return with the evidence before any real 3000 replacement. README/images, commits, tags, GitHub/GHCR publication, and release follow only after the runtime gates pass.

## Qualification Checkpoint: 2026-08-01 18:29

### Version Alignment And Candidate Build

- Version sources now report `0.2.1` in `package.json`, both root package-lock records, the backend platform fallback, SCIM runtime error text, release compatibility tests, and current-release comments/docstrings. Historical release and dependency references were not rewritten.
- Host release-version, branding, update, and subscription guards passed. Focused frontend Vitest passed 4 files / 8 tests, Python compilation passed, and the production frontend build completed with 1,731 SSR and 6,457 client modules. The inherited Svelte, chunk-size, Pyodide externalization, and missing-tsconfig warnings remain non-blocking.
- A second clean full-Dockerfile 0.2.1 build could not reach Docker Hub base-image OAuth metadata for `node:22-alpine3.20` and `python:3.11-slim-bookworm`. This failure occurred before project compilation and remains a publication gate.
- The private qualification image is therefore `artichat:0.2.1-rc-20260801-overlay`, built from the previously complete `artichat:0.2.0-about-20260801-full-retry-cache` image ID `sha256:b39b9022d4630b033d8ae843fd60a5b6fc7dc3b0217c92f130bc5a3dc4335c02` by replacing the current adapter-static `build/`, backend, and package manifest. Its local Docker index digest is `sha256:b5cdc0610f5d3a7036f91d907de5a0cc33fde803a342135d333bef43303f9428`, and its build hash is `local-0.2.1-rc-20260801-overlay`.
- This overlay is exact for the current application payload and dependencies, but it is private qualification-only. Do not publish or treat it as a substitute for the required clean network-available full Dockerfile rebuild.

### Exact-Image Verification And Private Runtime

- The exact overlay image passed the dependency-complete backend suite: `306 passed, 9 warnings`. Migration readiness passed `11 passed, 3 warnings`.
- Empty-volume smoke passed `/health`, `/ready`, `/api/version`, and `/` with HTTP 200, version `0.2.1`, and Alembic head `c3d4e5f6a7b8`. The adapter-static root layout is correct: `/app/build/index.html` exists and `/app/build/client` plus `/app/build/server` are absent.
- Port 3003 was health-gated onto the overlay while retaining the private volume `artichat_0_2_upgrade_copy_20260729`. The previous complete 0.2.0 candidate is retained stopped as `artichat-0.2.0-upgrade-copy-0730-rollback-full-20260801` on `artichat:0.2.0-about-20260801-full-retry-cache`.
- Authenticated Operations acceptance passed desktop 1440px and mobile 390px for Model Access, Redemption Codes, Gift Cards, Announcements, and Usage Ledger. It exercised the gift-recipient modal, exact default announcement cover, user search, registration-to-present filtering, and both ledger tabs with zero page/console errors and zero HTTP 5xx responses.
- About acceptance passed desktop and mobile with the expected five configured lines, no removed legacy blocks, no horizontal overflow, zero page/console errors, and zero HTTP 5xx responses.
- At the final check, 3003 `/health`, `/ready`, and `/api/version` returned 200 and version `0.2.1`. Production 3000 returned the same health statuses with version `0.1.7` and remained untouched.

### Direct 0.1.7 To 0.2.1 Upgrade Proof

- Production `artichat_data` was mounted read-only only long enough to create a disposable online SQLite-safe copy. The source snapshot contained main `webui.db` at 4,530,176 bytes, Chroma `chroma.sqlite3` at 806,912 bytes, 102 files, and 1,096,794,683 total bytes.
- Privacy-safe pre-upgrade fingerprints recorded 56 main tables / 845 rows / 49 retained files and 21 Chroma tables / 742 rows. Main foreign-key findings were zero; Chroma's existing eight findings were recorded as the baseline.
- The exact 0.2.1 overlay started directly on a disposable 0.1.7 copy, applied migrations from old head `c2d3e4f5a6b7` to `c3d4e5f6a7b8`, and became healthy without running an intermediate 0.2.0 runtime.
- Post-upgrade main verification checked 844 immutable rows, allowed only `alembic_version`, found the one expected new table, and found no missing or changed durable rows, columns, or retained files. The only new retained file was the expected persistent `.webui_secret_key` generated because the 0.1.7 source had none.
- Chroma preserved all pre-upgrade rows, tables, columns, and the existing eight foreign-key findings. Startup appended only two `acquire_write` coordination rows; verification allowed append-only rows in that table and rejected missing or unexpected mutations.
- In-volume checks reported SQLite integrity `ok`, zero main foreign-key findings, migration head `c3d4e5f6a7b8`, all required tables, and a persistent application key. A restart performed zero migrations, generated zero new keys, produced no critical log matches, and remained ready on 0.2.1.
- Authenticated smoke returned HTTP 200 for auth, chats, files, knowledge, current subscription, and subscription usage APIs. About passed at 1440px and 390px with no overflow, page/console errors, or HTTP 5xx responses.

### Rollback Rehearsal And Cleanup

- A pre-upgrade rollback archive was created from the source copy, measured 987,507,875 bytes, and had SHA-256 `e846b38244e2cb0e07b6b35bdb733dfcc81d33a6cdd0d893eaeb48f01d7a112b`. It restored into a fresh volume with exact pre-start main and Chroma fingerprints.
- Retained image `artichat:0.1.7-reasoning-control` started healthy from the restored volume on the old Alembic head `c2d3e4f5a6b7`; `/health`, `/ready`, `/api/version`, and `/` returned HTTP 200 with version `0.1.7` and no critical log matches.
- Final rollback verification preserved all 845 main rows, 56 main tables, 49 retained files, 742 pre-existing Chroma rows, 21 Chroma tables, and both integrity/foreign-key baselines. The only permitted effects of the rehearsal were one persistent `.webui_secret_key` file and one append-only `acquire_write` row.
- Disposable direct-upgrade and rollback containers, the three temporary clone/restore volumes, and the 987.5 MB raw archive were deleted after verification. The privacy-safe count/fingerprint JSON remains only under the local temporary evidence directory. Production volumes `artichat_data` and `artichat_data_backup_pre_gift_20260724`, plus private candidate volume `artichat_0_2_upgrade_copy_20260729`, were retained.

### Current Gate And Next Resume Point

1. The direct copied-volume runtime gate is green. Do not replace port 3000 until the user explicitly approves that separate operation.
2. Before GitHub/GHCR publication, rerun the complete Dockerfile build when Docker Hub is reachable and rerun the exact-image dependency, migration, empty-volume, and copied-volume checks against the resulting digest. The current overlay must not be published.
3. `README.md` and `artivis-ass/` remain unchanged. No commit, push, tag, release, publication, or production data mutation occurred.
4. Active worktree remains the historically named `.worktrees/artichat-0.2.0`, branch `artichat-0.2.0`, HEAD `50147b3a6a2c7ba7b5bfc0c983463d7d61209fb7`, with 347 expected dirty entries at this checkpoint.

## Publication Preparation Checkpoint: 2026-08-01 23:43

- The remote `origin/main` history at `09f9e99a5` was fetched and merged into the release branch before final edits. Its seven README and `artivis-ass/` asset commits are retained; the release branch is not based on a stale remote tip.
- A clean complete Dockerfile build succeeded after Docker Hub connectivity recovered. The exact local image is `artichat:0.2.1-full-20260801`, manifest digest `sha256:4950a67e42d835415675b08e9f648a94383f3d079d5c2f972feda9442a1fe2b3`, build hash `local-0.2.1-full-20260801`. The build used official cached base digests: Node `sha256:2289fb1fba0f4633b08ec47b94a89c7e20b829fc5679f9b7b298eaa2f1ed8b7e` and Python `sha256:b18992999dbe963a45a8a4da40ac2b1975be1a776d939d098c647482bcad5cba`.
- The exact full image passed `412 passed, 24 warnings` with a read-only Dockerfile mount for the static healthcheck assertion, and the focused migration readiness suite passed `11 passed, 3 warnings`. Empty-volume startup passed all endpoints with version `0.2.1` and head `c3d4e5f6a7b8`.
- Private port 3003 now runs the exact full image on `artichat_0_2_upgrade_copy_20260729`; the prior overlay remains stopped as `artichat-0.2.0-upgrade-copy-0730-rollback-overlay-20260801`. Authenticated desktop/mobile Operations and About checks on the full image passed with zero page/console errors, zero HTTP 5xx, and no horizontal overflow. Port 3000 remains the healthy untouched 0.1.7 runtime.
- README now advertises 0.2.1, documents direct 0.1.7 upgrade and archive-based rollback, and describes the persistent secret-key file. `docs/releases/0.2.1.md` contains the release body. The 15 README-local image references all resolve.
- Public screenshots were reviewed. `artivis-ass/new/user.png` now contains only example.com placeholder emails; `a1.png` shows About 0.2.1; `a3.png` replaces the private image API endpoint with `https://api.example.com/v1`. Other supplied images contain no visible credentials or tokens in the reviewed surfaces.
- GitHub Actions workflow `.github/workflows/artichat-release.yml` is configured to build/push GHCR and create the GitHub Release when `v0.2.1` is pushed. A tag/release does not update `main` automatically; the final commit must be explicitly pushed to `origin/main` first.
- No GitHub push, tag, release, GHCR publication, or production port-3000 replacement has occurred yet. The next action is final staged review, commit, fast-forward `origin/main`, then push `v0.2.1` and monitor the release workflow.

## Published Checkpoint: 2026-08-02 01:25

- Final release commit is `cabb44a17e557afe6275fcc5a8dac76df021243c` (`ci: raise release build memory limit`). It adds `NODE_OPTIONS=--max-old-space-size=8192` to the release workflow after the first GitHub runner build reached 6,457 modules and exhausted its default Node heap during SSR chunk rendering.
- `origin/main` and `origin/v0.2.1` both resolve to `cabb44a17e557afe6275fcc5a8dac76df021243c`. The previous upstream local tag object remains preserved as `openwebui-v0.2.1` at `cfc78dedf0024ecb97a5090825af2698524c9d71`.
- GitHub Actions `ArtiChat Release` run `30709774923` completed successfully: release guards, frontend tests, 8 GB frontend build, Buildx image build/push, published image inspection, and GitHub Release creation all passed. The earlier Python CI failure was the repository-wide Ruff-format baseline (`54 files would be reformatted`), not a release-workflow failure.
- GitHub Release is published at `https://github.com/PYBu/ArtiChat/releases/tag/v0.2.1`, non-draft and non-prerelease, using `docs/releases/0.2.1.md`.
- GHCR tag `ghcr.io/pybu/artichat:0.2.1` is published. Registry manifest includes Linux amd64 digest `sha256:8f316b0e2d05a5d74a0a210e3e1d3df3d5f96c0e5d8a330da9451841a9c44e61`.
- Final local worktree is clean. Port 3000 remains healthy on version `0.1.7`; private port 3003 remains healthy on version `0.2.1`. No production volume was migrated or modified, and no deployment workflow was triggered.
