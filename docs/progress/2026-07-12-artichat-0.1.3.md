# ArtiChat 0.1.3 Progress

## Milestone 1: Bug Fixes And Admin Page Restructure

- Status: implementation and automated regression complete; final browser acceptance is consolidated into the local test stage after all 0.1.3 milestones.
- Model permissions use one atomic bulk save.
- Deleted announcements are hidden by default and remain available through inactive history.
- Subscription operations use independent admin routes with visible mobile navigation.
- Primary ArtiChat logo surfaces select light and dark assets from the active theme.
- Full subscription backend suite: 45 passed, with one existing SQLAlchemy deprecation warning.
- Subscription and branding source guards: passed.
- Frontend Vitest suite: 3 passed.
- Production Vite build: passed.
- Full `svelte-check`: currently blocked by pre-existing diagnostics in unrelated baseline files, including `RichTextInput/AutoCompletion.js`, `RichTextInput/listDragHandlePlugin.js`, and `chat/Settings/Advanced/AdvancedParams.svelte`.

## Milestone 2: Four-Part Pricing And Usage Ledger

- Status: implementation, migration rehearsal, and automated regression complete; final browser acceptance remains in the consolidated local test stage after milestone 5.
- Model policies now define input, output, cache-creation, and cache-read prices in `Chatpoint / 1M Token`.
- Legacy `usage_multiplier` remains compatible and maps to equivalent input/output prices at `100 * multiplier`; cache prices start at zero.
- Provider usage is normalized into four non-overlapping Token categories. OpenAI cached prompt subsets are removed from normal input while Anthropic cache buckets remain separate.
- Metered balance debit, balance ledger insertion, and usage insertion use one database commit and roll back together on failure.
- New usage records snapshot all four prices, raw provider usage, request ID, first-content latency, total duration, status, and audit IP.
- User usage responses use an explicit safe projection and cannot serialize IP, raw usage, internal metadata, or another user summary.
- Admin usage supports user ID, model, status, and date filters and displays full IP audit data.
- Alembic revision: `a4b5c6d7e8f9`.
- Empty SQLite upgrade rehearsal: completed through `a4b5c6d7e8f9 (head)`.
- Full subscription backend suite: 69 passed, with one existing SQLAlchemy deprecation warning.
- Subscription and branding source guards: passed.
- Frontend Vitest suite: 3 passed.
- Production Vite build: passed.
- Full `svelte-check`: remains blocked by the same pre-existing unrelated baseline diagnostics recorded in milestone 1.

## Next

## Milestone 3: User And Platform Administration

- Status: implementation and automated regression complete; final browser acceptance remains in the consolidated local test stage after milestone 5.
- Admin user rows include subscription name and expiry summaries from one bulk query.
- The edit-user modal directly edits tier, status, expiry, Plan Chatpoint, Check Chatpoint, and administrator notes.
- The old standalone user-subscription page and subscription-home entry were removed; compatibility APIs remain available.
- Platform settings can change the global name, About title/content, and separate light/dark theme logos.
- Logo uploads accept PNG, JPEG, or WebP up to 2 MiB, decode with Pillow, and re-encode to fixed server-owned PNG files under the persistent data directory.
- Public frontend config exposes only platform branding URLs and text, never server filesystem paths.
- ThemeLogo prefers configured assets and falls back to packaged ArtiChat assets.
- About displays custom title/content before the ArtiChat version line.
- Full subscription/platform backend suite: 75 passed, with one existing SQLAlchemy deprecation warning.
- Subscription and branding source guards: passed.
- Frontend Vitest suite: 3 passed.
- Production Vite build: passed.
- Full `svelte-check`: remains blocked by the same pre-existing unrelated baseline diagnostics recorded in milestones 1 and 2.

## Next

Implement milestone 4: registration-domain controls, SMTP configuration, customizable email templates, verification codes, email login, password reset, and sensitive-action verification.

## Milestone 4: Registration, Email, And Account Security

- Status: implementation and automated regression complete on the isolated `artichat-brand-whitelabel` worktree; local runtime acceptance is in progress, with no GitHub push or production changes.
- Registration domain normalization, optional subdomain matching, email-code/reset-token secret helpers, and additive security tables are complete in commits `7daef7f`, `3c91c5f`, and `30f3514`.
- SMTP configuration foundation now derives Fernet encryption from `WEBUI_SECRET_KEY`, preserves an existing encrypted password on masked updates, and never includes ciphertext in the admin projection.
- SMTP security modes are limited to plain SMTP, STARTTLS, and implicit SSL with validated ports.
- Nine required default email templates now use explicit per-template variable allowlists and render one escaped HTML body plus one plain-text body.
- SMTP transport now supports plain SMTP, STARTTLS, and implicit SSL; connection, TLS, authentication, and send failures expose fixed stage codes without leaking server details or credentials.
- Every send creates a delivery row, increments attempts, records sent/failed state, redacts codes and reset links from stored variables, and allows failed deliveries to be retried.
- Plain-text email rendering preserves link targets so password-reset links work without HTML.
- Config now stores the email switch, SMTP connection fields, encrypted password, sender identity, public URL, and subscription-notification switch with safe defaults.
- Admin-only email APIs cover masked settings, connection testing, real test mail, template list/update, delivery history, and failed-delivery retry under `/api/v1/emails/admin/*`.
- Admin email management is split into independent `/admin/email/settings`, `/admin/email/templates`, and `/admin/email/deliveries` pages with a mobile-visible page selector and top-level admin navigation.
- Email settings supports save, connection test, and real test mail; templates expose only allowed variables; delivery history provides retry only for failed messages.
- Milestone 4 Task 2 regression: full subscription backend suite 109 passed, frontend Vitest 5 passed, subscription source guard passed, and production Vite build passed.
- Full `svelte-check` remains blocked by the existing unrelated baseline; filtered diagnostics contain no new email API, component, or route errors.
- Email challenge lifecycle now enforces a 60-second resend cooldown, five sends per email/hour, twenty sends per IP/hour, ten-minute expiry, five failed attempts, purpose-bound HMAC verification, consume-on-success, and replay rejection.
- Full subscription backend suite after the challenge lifecycle: 112 passed, with one existing SQLAlchemy deprecation warning.
- Registration management now persists normalized domain allowlists, optional subdomains, registration verification, email-code login, and sensitive-action verification switches.
- `/admin/registration` is an independent desktop/mobile-visible page; the public registration projection exposes only the two auth-screen feature switches and never the allowlist.
- Registration configuration slice: 18 focused backend tests passed and the production Vite build passed.
- Public registration/login code requests now apply feature switches and registration domain rules, return the same success shape for existing-registration and missing-login accounts, and never expose the raw code.
- Codes are dispatched through the matching customizable template; a failed delivery immediately consumes the new challenge so an unsent code cannot remain valid.
- Full subscription backend suite after challenge dispatch: 116 passed, with one existing SQLAlchemy deprecation warning.
- Public challenge verification now consumes a valid code and returns only its random challenge ID as a short-lived verification ticket; replay returns `EMAIL_CODE_ALREADY_USED`.
- Full subscription backend suite after verification tickets: 117 passed, with one existing SQLAlchemy deprecation warning.
- Users now persist `email_verified_at`; verification tickets persist a separate `claimed_at` marker and can be claimed by exactly one account action.
- First-admin signup remains available without SMTP, while later signups enforce the configured domain policy and require a matching consumed registration ticket when verification is enabled.
- Email-code login is an alternate two-step login mode that creates the normal session after claiming a matching login ticket; password, OAuth, and LDAP paths remain available.
- Alembic revision `c6d7e8f9a0b1` adds the user verification timestamp and ticket-claim state; an empty SQLite upgrade rehearsal reached `c6d7e8f9a0b1 (head)`.
- Task 3 regression: full subscription backend suite 123 passed, frontend Vitest 6 passed, and the production Vite build passed.
- Password reset now uses a generic forgot response, HMAC-only reset-token storage, a 30-minute lifetime, automatic invalidation of older links, and one-time consumption.
- The login page links to an independent forgot-password page; reset emails lead to an independent reset page with password confirmation and actionable invalid/expired errors.
- Successful resets update the credential and send the customizable password-changed security notice without allowing mail failure to undo the password change.
- Password hashing/validation was separated into a dependency-light utility while remaining re-exported from the existing auth module for compatibility.
- Password-reset slice: full subscription backend suite 130 passed, frontend Vitest 6 passed, Python compilation passed, and the production Vite build passed.
- Password reset now rotates a persistent per-user authentication epoch, deletes OAuth sessions, updates Redis revocation state when available, and disconnects realtime sessions.
- Every password, email-code, LDAP, OAuth, admin-created, and automation JWT carries the current authentication epoch; legacy users remain compatible until their first security rotation.
- Alembic revision `d7e8f9a0b1c2` adds the nullable authentication epoch and is the single migration head.
- Persistent session-revocation slice: full subscription backend suite 132 passed and affected Python modules compiled successfully.
- Sensitive-action codes now bind to the user ID, current JWT `jti`, and exact action; the resulting 10-minute grant is one-use and cannot move between devices or between password and billing changes.
- Password and payment-information endpoints preserve their old behavior while verification is disabled and require a claimed grant when the administrator enables sensitive-action verification.
- User settings now run the send-code and verify-code steps inline before saving password or payment information, with clear disabled-mail and invalid/expired-code errors.
- Sensitive password/billing slice: full subscription backend suite 134 passed, email API Vitest passed, and the production Vite build passed.
- Login-email changes now require a current-email grant before sending a code to the proposed address; the second grant is bound to that exact address, user, session, and action.
- Auth and User email rows update in one database transaction, the new address is marked verified, both old and new addresses receive the customizable change notice, and all prior sessions are revoked.
- Account settings expose the full two-stage flow and return to login after success because the prior JWT is intentionally invalid.
- Task 4 regression: full subscription backend suite 135 passed, frontend Vitest 6 passed, Python compilation passed, and the production Vite build passed.
- Normal password changes and payment-information changes now send their customizable security notices after the database update succeeds.
- Redemption, gift-card claim, and administrator subscription changes send subscription notices only when the dedicated admin switch is enabled.
- The shared notification service absorbs disabled SMTP, disabled subscription notices, template errors, and transport failures so committed business changes are never rolled back.
- Notification slice: full subscription backend suite 137 passed and affected Python modules compiled successfully.
- Login registration/code flows now show a live 60-second resend cooldown, a 10-minute expiry state, and an enabled resend action when the cooldown ends.
- Password and payment-information verification can resend codes; two-stage login-email verification can restart cleanly after expiry or failed attempts.
- Security/configuration error codes now map to actionable Chinese messages, while delivery history retains explicit failed-mail retry controls and SMTP settings retain stage-specific diagnostics.
- When a user has at least one pending gift card, the expanded sidebar shows an unnumbered `可领取的礼品` button directly above the avatar and opens the existing redemption/gift-claim view.
- UI-completion regression: full subscription backend suite 137 passed, frontend Vitest 8 passed, and the production Vite build passed.
- The release version is now `0.1.3`; the explicit release-version guard passed for tag `v0.1.3` and channel `Artivis Alpha`.
- Final source guards passed for branding, subscriptions, and the update system.
- The empty SQLite migration rehearsal reached the single head `d7e8f9a0b1c2`.
- The complete milestone regression passed 137 backend tests and 8 frontend tests; the production Vite build passed.
- The global Svelte-check baseline remains 9,447 errors and 273 warnings across 382 pre-existing files; affected source still compiles in the production build.
- Docker image `artichat:0.1.3-local` built successfully with image ID `sha256:df53a3fd6aaf45459413d1f95b855306c11b34814548eb241589273c63a37224`. The build reported only the two pre-existing Dockerfile secret-in-ENV policy warnings.
- A localhost-only disposable container passed `/health`, root-page, and authenticated desktop/mobile browser acceptance across platform, registration, email, subscription, usage, and user administration routes without page or console errors or horizontal overflow.
- Browser acceptance confirmed the dark theme loads `/static/favicon-dark.png`, the mobile admin selector exposes the independent subscription/registration/email/settings pages, and the gift entry appears unnumbered directly above the avatar.
- Gift claiming now uses the existing subscription refresh tick to re-query pending gifts. A browser regression confirmed the sidebar entry disappears immediately after a successful claim without reload or window focus; the frontend suite now passes 9 tests.
- The pre-GitHub file audit found no untracked files, runtime databases/logs, tracked real environment files, private-key/API-key patterns, build output, or test artifacts.
- Removed unreferenced root `demo.png` and `banner.png`; both were stale Open WebUI-branded marketing images and were not used by code, documentation, or builds.
- Large tracked assets were reviewed: login backgrounds, document-export fonts, Swagger assets, SQL WASM, and dependency locks all have runtime or reproducibility roles and remain.
- Independent release review found four Important security/concurrency gaps; all were reproduced before implementation and closed with focused regressions.
- Non-actionable registration/login requests now create rate-limited decoy challenges without sending mail, so repeated request cooldowns cannot reveal account existence.
- Password-reset consumption, verification-code consumption, verification-ticket claims, and sensitive-action grants now use conditional atomic updates so concurrent replay has exactly one winner.
- Metered billing obtains a database write lock on the user subscription row and reloads the balance before debit calculation; two concurrent 10 CP requests now reduce 100 CP to 80 CP instead of losing one debit.
- Security delivery rows store only bodies rendered with redacted codes/reset links. Those failed credentials cannot be retried in the API or admin UI; ordinary notification/test deliveries remain retryable.
- Post-review regression: full subscription backend suite 141 passed and frontend Vitest suite 10 passed.

## Current Resume Point

Rebuild the 0.1.3 image with the post-review security fixes, then replace the existing local `artichat` container on port 3000 while preserving its `artichat_data` volume, secret, environment, and restart policy. Remove the disposable port-3001 container only after port 3000 is healthy. The repository-file audit is complete; do not push until the user finishes local testing and explicitly approves it. `static/pyodide/pyodide-lock.json` is intentionally tracked for reproducible/faster builds, so preserve its unrelated working-tree deletion without committing it and review that deletion with the user before GitHub upload.
