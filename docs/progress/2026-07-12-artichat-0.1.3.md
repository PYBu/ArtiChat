# ArtiChat 0.1.3 Progress

## Milestone 1: Bug Fixes And Admin Page Restructure

- Status: implementation and automated regression complete; final browser acceptance is consolidated into the local test stage after all 0.1.3 milestones.
- Model permissions use one atomic bulk save.
- Deleted announcements are hidden by default and remain available through inactive history.
- Subscription operations use independent admin routes with visible mobile navigation.
- Primary ArtiChat logo surfaces select light and dark assets from the active theme.
- Full subscription backend suite: 45 passed, with one existing SQLAlchemy deprecation warning.
- Subscription and branding source guards: passed.
- Frontend Vitest suite: 3 passed.
- Production Vite build: passed.
- Full `svelte-check`: currently blocked by pre-existing diagnostics in unrelated baseline files, including `RichTextInput/AutoCompletion.js`, `RichTextInput/listDragHandlePlugin.js`, and `chat/Settings/Advanced/AdvancedParams.svelte`.

## Milestone 2: Four-Part Pricing And Usage Ledger

- Status: implementation, migration rehearsal, and automated regression complete; final browser acceptance remains in the consolidated local test stage after milestone 5.
- Model policies now define input, output, cache-creation, and cache-read prices in `Chatpoint / 1M Token`.
- Legacy `usage_multiplier` remains compatible and maps to equivalent input/output prices at `100 * multiplier`; cache prices start at zero.
- Provider usage is normalized into four non-overlapping Token categories. OpenAI cached prompt subsets are removed from normal input while Anthropic cache buckets remain separate.
- Metered balance debit, balance ledger insertion, and usage insertion use one database commit and roll back together on failure.
- New usage records snapshot all four prices, raw provider usage, request ID, first-content latency, total duration, status, and audit IP.
- User usage responses use an explicit safe projection and cannot serialize IP, raw usage, internal metadata, or another user summary.
- Admin usage supports user ID, model, status, and date filters and displays full IP audit data.
- Alembic revision: `a4b5c6d7e8f9`.
- Empty SQLite upgrade rehearsal: completed through `a4b5c6d7e8f9 (head)`.
- Full subscription backend suite: 69 passed, with one existing SQLAlchemy deprecation warning.
- Subscription and branding source guards: passed.
- Frontend Vitest suite: 3 passed.
- Production Vite build: passed.
- Full `svelte-check`: remains blocked by the same pre-existing unrelated baseline diagnostics recorded in milestone 1.

## Next

## Milestone 3: User And Platform Administration

- Status: implementation and automated regression complete; final browser acceptance remains in the consolidated local test stage after milestone 5.
- Admin user rows include subscription name and expiry summaries from one bulk query.
- The edit-user modal directly edits tier, status, expiry, Plan Chatpoint, Check Chatpoint, and administrator notes.
- The old standalone user-subscription page and subscription-home entry were removed; compatibility APIs remain available.
- Platform settings can change the global name, About title/content, and separate light/dark theme logos.
- Logo uploads accept PNG, JPEG, or WebP up to 2 MiB, decode with Pillow, and re-encode to fixed server-owned PNG files under the persistent data directory.
- Public frontend config exposes only platform branding URLs and text, never server filesystem paths.
- ThemeLogo prefers configured assets and falls back to packaged ArtiChat assets.
- About displays custom title/content before the ArtiChat version line.
- Full subscription/platform backend suite: 75 passed, with one existing SQLAlchemy deprecation warning.
- Subscription and branding source guards: passed.
- Frontend Vitest suite: 3 passed.
- Production Vite build: passed.
- Full `svelte-check`: remains blocked by the same pre-existing unrelated baseline diagnostics recorded in milestones 1 and 2.

## Next

Implement milestone 4: registration-domain controls, SMTP configuration, customizable email templates, verification codes, email login, password reset, and sensitive-action verification.

## Milestone 4: Registration, Email, And Account Security

- Status: implementation and automated regression complete on the isolated `artichat-brand-whitelabel` worktree; local runtime acceptance is in progress, with no GitHub push or production changes.
- Registration domain normalization, optional subdomain matching, email-code/reset-token secret helpers, and additive security tables are complete in commits `7daef7f`, `3c91c5f`, and `30f3514`.
- SMTP configuration foundation now derives Fernet encryption from `WEBUI_SECRET_KEY`, preserves an existing encrypted password on masked updates, and never includes ciphertext in the admin projection.
- SMTP security modes are limited to plain SMTP, STARTTLS, and implicit SSL with validated ports.
- Nine required default email templates now use explicit per-template variable allowlists and render one escaped HTML body plus one plain-text body.
- SMTP transport now supports plain SMTP, STARTTLS, and implicit SSL; connection, TLS, authentication, and send failures expose fixed stage codes without leaking server details or credentials.
- Every send creates a delivery row, increments attempts, records sent/failed state, redacts codes and reset links from stored variables, and allows failed deliveries to be retried.
- Plain-text email rendering preserves link targets so password-reset links work without HTML.
- Config now stores the email switch, SMTP connection fields, encrypted password, sender identity, public URL, and subscription-notification switch with safe defaults.
- Admin-only email APIs cover masked settings, connection testing, real test mail, template list/update, delivery history, and failed-delivery retry under `/api/v1/emails/admin/*`.
- Admin email management is split into independent `/admin/email/settings`, `/admin/email/templates`, and `/admin/email/deliveries` pages with a mobile-visible page selector and top-level admin navigation.
- Email settings supports save, connection test, and real test mail; templates expose only allowed variables; delivery history provides retry only for failed messages.
- Milestone 4 Task 2 regression: full subscription backend suite 109 passed, frontend Vitest 5 passed, subscription source guard passed, and production Vite build passed.
- Full `svelte-check` remains blocked by the existing unrelated baseline; filtered diagnostics contain no new email API, component, or route errors.
- Email challenge lifecycle now enforces a 60-second resend cooldown, five sends per email/hour, twenty sends per IP/hour, ten-minute expiry, five failed attempts, purpose-bound HMAC verification, consume-on-success, and replay rejection.
- Full subscription backend suite after the challenge lifecycle: 112 passed, with one existing SQLAlchemy deprecation warning.
- Registration management now persists normalized domain allowlists, optional subdomains, registration verification, email-code login, and sensitive-action verification switches.
- `/admin/registration` is an independent desktop/mobile-visible page; the public registration projection exposes only the two auth-screen feature switches and never the allowlist.
- Registration configuration slice: 18 focused backend tests passed and the production Vite build passed.
- Public registration/login code requests now apply feature switches and registration domain rules, return the same success shape for existing-registration and missing-login accounts, and never expose the raw code.
- Codes are dispatched through the matching customizable template; a failed delivery immediately consumes the new challenge so an unsent code cannot remain valid.
- Full subscription backend suite after challenge dispatch: 116 passed, with one existing SQLAlchemy deprecation warning.
- Public challenge verification now consumes a valid code and returns only its random challenge ID as a short-lived verification ticket; replay returns `EMAIL_CODE_ALREADY_USED`.
- Full subscription backend suite after verification tickets: 117 passed, with one existing SQLAlchemy deprecation warning.
- Users now persist `email_verified_at`; verification tickets persist a separate `claimed_at` marker and can be claimed by exactly one account action.
- First-admin signup remains available without SMTP, while later signups enforce the configured domain policy and require a matching consumed registration ticket when verification is enabled.
- Email-code login is an alternate two-step login mode that creates the normal session after claiming a matching login ticket; password, OAuth, and LDAP paths remain available.
- Alembic revision `c6d7e8f9a0b1` adds the user verification timestamp and ticket-claim state; an empty SQLite upgrade rehearsal reached `c6d7e8f9a0b1 (head)`.
- Task 3 regression: full subscription backend suite 123 passed, frontend Vitest 6 passed, and the production Vite build passed.
- Password reset now uses a generic forgot response, HMAC-only reset-token storage, a 30-minute lifetime, automatic invalidation of older links, and one-time consumption.
- The login page links to an independent forgot-password page; reset emails lead to an independent reset page with password confirmation and actionable invalid/expired errors.
- Successful resets update the credential and send the customizable password-changed security notice without allowing mail failure to undo the password change.
- Password hashing/validation was separated into a dependency-light utility while remaining re-exported from the existing auth module for compatibility.
- Password-reset slice: full subscription backend suite 130 passed, frontend Vitest 6 passed, Python compilation passed, and the production Vite build passed.
- Password reset now rotates a persistent per-user authentication epoch, deletes OAuth sessions, updates Redis revocation state when available, and disconnects realtime sessions.
- Every password, email-code, LDAP, OAuth, admin-created, and automation JWT carries the current authentication epoch; legacy users remain compatible until their first security rotation.
- Alembic revision `d7e8f9a0b1c2` adds the nullable authentication epoch and is the single migration head.
- Persistent session-revocation slice: full subscription backend suite 132 passed and affected Python modules compiled successfully.
- Sensitive-action codes now bind to the user ID, current JWT `jti`, and exact action; the resulting 10-minute grant is one-use and cannot move between devices or between password and billing changes.
- Password and payment-information endpoints preserve their old behavior while verification is disabled and require a claimed grant when the administrator enables sensitive-action verification.
- User settings now run the send-code and verify-code steps inline before saving password or payment information, with clear disabled-mail and invalid/expired-code errors.
- Sensitive password/billing slice: full subscription backend suite 134 passed, email API Vitest passed, and the production Vite build passed.
- Login-email changes now require a current-email grant before sending a code to the proposed address; the second grant is bound to that exact address, user, session, and action.
- Auth and User email rows update in one database transaction, the new address is marked verified, both old and new addresses receive the customizable change notice, and all prior sessions are revoked.
- Account settings expose the full two-stage flow and return to login after success because the prior JWT is intentionally invalid.
- Task 4 regression: full subscription backend suite 135 passed, frontend Vitest 6 passed, Python compilation passed, and the production Vite build passed.
- Normal password changes and payment-information changes now send their customizable security notices after the database update succeeds.
- Redemption, gift-card claim, and administrator subscription changes send subscription notices only when the dedicated admin switch is enabled.
- The shared notification service absorbs disabled SMTP, disabled subscription notices, template errors, and transport failures so committed business changes are never rolled back.
- Notification slice: full subscription backend suite 137 passed and affected Python modules compiled successfully.
- Login registration/code flows now show a live 60-second resend cooldown, a 10-minute expiry state, and an enabled resend action when the cooldown ends.
- Password and payment-information verification can resend codes; two-stage login-email verification can restart cleanly after expiry or failed attempts.
- Security/configuration error codes now map to actionable Chinese messages, while delivery history retains explicit failed-mail retry controls and SMTP settings retain stage-specific diagnostics.
- When a user has at least one pending gift card, the expanded sidebar shows an unnumbered `可领取的礼品` button directly above the avatar and opens the existing redemption/gift-claim view.
- UI-completion regression: full subscription backend suite 137 passed, frontend Vitest 8 passed, and the production Vite build passed.
- The release version is now `0.1.3`; the explicit release-version guard passed for tag `v0.1.3` and channel `Artivis Alpha`.
- Final source guards passed for branding, subscriptions, and the update system.
- The empty SQLite migration rehearsal reached the single head `d7e8f9a0b1c2`.
- The complete milestone regression passed 137 backend tests and 8 frontend tests; the production Vite build passed.
- The global Svelte-check baseline remains 9,447 errors and 273 warnings across 382 pre-existing files; affected source still compiles in the production build.
- Docker image `artichat:0.1.3-local` built successfully with image ID `sha256:ada5d82d2f3bfac2fc2f58394c804da17b6f2024e21f660f41543c71197ba0bb`. The build reported only the two pre-existing Dockerfile secret-in-ENV policy warnings.
- A localhost-only disposable container passed `/health`, root-page, and authenticated desktop/mobile browser acceptance across platform, registration, email, subscription, usage, and user administration routes without page or console errors or horizontal overflow.
- Browser acceptance confirmed the dark theme loads `/static/favicon-dark.png`, the mobile admin selector exposes the independent subscription/registration/email/settings pages, and the gift entry appears unnumbered directly above the avatar.
- Gift claiming now uses the existing subscription refresh tick to re-query pending gifts. A browser regression confirmed the sidebar entry disappears immediately after a successful claim without reload or window focus; the frontend suite now passes 9 tests.
- The pre-GitHub file audit found no untracked files, runtime databases/logs, tracked real environment files, private-key/API-key patterns, build output, or test artifacts.
- Removed unreferenced root `demo.png` and `banner.png`; both were stale Open WebUI-branded marketing images and were not used by code, documentation, or builds.
- Large tracked assets were reviewed: login backgrounds, document-export fonts, Swagger assets, SQL WASM, and dependency locks all have runtime or reproducibility roles and remain.
- Independent release review found four Important security/concurrency gaps; all were reproduced before implementation and closed with focused regressions.
- Non-actionable registration/login requests now create rate-limited decoy challenges without sending mail, so repeated request cooldowns cannot reveal account existence.
- Password-reset consumption, verification-code consumption, verification-ticket claims, and sensitive-action grants now use conditional atomic updates so concurrent replay has exactly one winner.
- Metered billing obtains a database write lock on the user subscription row and reloads the balance before debit calculation; two concurrent 10 CP requests now reduce 100 CP to 80 CP instead of losing one debit.
- Security delivery rows store only bodies rendered with redacted codes/reset links. Those failed credentials cannot be retried in the API or admin UI; ordinary notification/test deliveries remain retryable.
- Post-review regression: full subscription backend suite 141 passed and frontend Vitest suite 10 passed.
- The existing local `artichat` container was replaced through a health-gated rollback swap while preserving the `artichat_data` volume, complete environment, port 3000 binding, and `unless-stopped` policy. The stale inherited build hash was removed so `/api/version` reports `local-0.1.3`.
- Final port-3000 checks: container healthy, `/health` true, version `0.1.3`, title `ArtiChat`, no recent runtime error patterns, and desktop/mobile login pages have no page errors or horizontal overflow. The disposable port-3001 container and volume were removed.

## User Acceptance

- The user completed local acceptance testing against the existing-data container at `http://localhost:3000` and confirmed the project is ready to move to the GitHub preparation stage in the next conversation.
- No GitHub push, tag, release, merge, or production deployment has been performed.
- The `artichat-brand-whitelabel` branch and linked worktree remain intact for follow-up work.

## Current Resume Point

Local user acceptance is complete. In the next conversation, open `C:\Users\admin\Desktop\Pro\ArtiChat\.worktrees\artichat-brand-whitelabel`, read this progress file, and verify the healthy port-3000 container plus the current branch/status. Before any GitHub action, resolve the unrelated deletion of `static/pyodide/pyodide-lock.json`: it is intentionally tracked for reproducible/faster builds and has not been included in any 0.1.3 commit. Then review the exact upload diff and release metadata with the user. Do not push, tag, merge, publish a release, or touch production without explicit approval.

## 2026-07-13 HTML Email Follow-up

- Email templates now persist an optional safe HTML body while retaining legacy Markdown compatibility and a plain-text alternative.
- The nine built-in templates use an ArtiChat-style card with the configured platform Logo, platform name, action content, and `{platform_name} v{platform_version}` footer.
- The admin template page supports HTML editing, allowed-variable insertion, and a sandboxed server-rendered preview.
- Alembic revision `e8f9a0b1c2d3` adds `email_template.html_body` and is the single migration head.
- Existing default Markdown templates are upgraded to the branded HTML defaults only when their stored Markdown still exactly matches the built-in default, preserving customized templates.
- Regression passed: 143 subscription/backend tests, 10 frontend Vitest tests, production Vite build, release/source guards, Python compilation, and empty-database migration rehearsal.
- Desktop and mobile browser acceptance of the admin preview passed without page errors, console errors, or horizontal overflow.
- The current worktree was built as `artichat:0.1.3-local` with build hash `local-0.1.3-email-html` and deployed to `http://localhost:3000` through a health-gated swap while preserving `artichat_data`.
- Existing data now has nine HTML-backed email templates and migration head `e8f9a0b1c2d3`; the port-3000 container is healthy and awaits user acceptance.
- Repository-level [AGENTS.md](../../AGENTS.md) now records the durable rule that every completed implementation must be deployed to port 3000 for user acceptance, with data preservation and health-gated rollback.
- No GitHub push, tag, release, merge, or production deployment has been performed.

## Current Resume Point (2026-07-13)

User acceptance is pending at `http://localhost:3000` for the branded HTML email-template follow-up. Future implementation tasks must follow the repository-level `AGENTS.md` local-acceptance workflow before completion is reported.

## 2026-07-13 Overall Optimization

- Default email-template seeding now loads existing rows once instead of running one lookup per template, and it commits only when rows are created or legacy defaults are upgraded.
- Default subscription-plan seeding no longer marks already-current names and descriptions as changed or performs an empty commit on every plan request.
- Redemption-code batches generate and hash candidates in memory, detect in-batch duplicates, and check all hashes with one database query instead of one query per code.
- Subscription usage totals now use database aggregation across the complete active filter, independent of item pagination; usage rows are validated only once before admin projection.
- Admin list limits are bounded and redemption batches are capped at 500 codes; gift-card recipient lists are capped at 1,000 users.
- Model-policy GET, bulk PUT, and single PUT endpoints now return the same explicit projection, so frontend state no longer changes shape after a save.
- A shared typed JSON request layer now covers announcements, subscriptions, email, and updates with consistent auth headers, stable error codes, custom-header preservation, and abort-signal support.
- Announcement, subscription, usage, redemption, and subscription-admin views now use domain response types instead of branch-added `any` values.
- Email preview requests cancel and ignore superseded responses, preventing stale previews during rapid template switches or refreshes.
- Docker excludes local `build`, `.svelte-kit`, test caches, Python bytecode, and `deploy-backups`; final build context fell from about 313 MB to about 332 KB.
- Pyodide is pinned to the installed lock version `0.28.3`, and the preparation script compares the actual installed package version; repeat preparation no longer deletes the valid cache and completed in about 16 seconds locally.
- Regression passed: 149 subscription/backend tests, 13 frontend Vitest tests, targeted ESLint, all four source/release guards, Python compilation, affected-file Svelte diagnostics, and production Docker build.
- Isolated desktop/mobile browser acceptance covered announcement, plan, model-policy, usage, and email-template pages without page errors, console errors, or horizontal overflow; branded email preview also passed.
- The optimized image is running at `http://localhost:3000` with build hash `local-0.1.3-optimized`, the existing `artichat_data` volume, healthy status, and zero restarts.
- No GitHub push, tag, release, merge, or production deployment has been performed.

## Current Resume Point (Overall Optimization)

The optimized 0.1.3 worktree is running at `http://localhost:3000` and awaits user acceptance. Continue to follow `AGENTS.md`: every completed implementation must be health-gated onto port 3000 with existing data preserved before completion is reported.

## 2026-07-13 Release Announcement And GitHub Preparation

- GitHub release notes in `docs/releases/0.1.3.md` now provide a user-focused summary, detailed management/email/security/performance changes, external Ollama compatibility guidance, database and SMTP upgrade notes, and the verified test scope.
- Alembic revision `f9a0b1c2d3e4` inserts a deterministic `ann_release_0_1_3` announcement after the HTML-email migration. It is shown once per user, does not overwrite administrator announcements, is idempotent, and removes only its own row on downgrade.
- The in-app announcement highlights account recovery, SMTP and branded HTML email, subscription and usage management, four-part Token pricing, configurable branding, and the database-backup requirement.
- The release-announcement migration has a focused SQLite regression covering repeat upgrade and downgrade behavior.
- Release regression passed: 150 subscription/backend tests, 13 frontend tests, release-version/branding/subscription/update guards, Python compilation, and a complete production Docker build.
- Image `artichat:0.1.3-upload-candidate` (`sha256:c1a2bd92ac3e94553bd4f3eb2c3fbcb60768449da91cbfa556b84221c804d26d`) was built with hash `local-0.1.3-release-announcement` and health-gated onto `http://localhost:3000` with `artichat_data` preserved.
- Port-3000 verification passed: healthy with zero restarts, version `0.1.3 (Artivis Alpha)`, correct build hash and title, Alembic head `f9a0b1c2d3e4`, one release-announcement row, three existing users, and all nine email templates retaining HTML bodies.
- The GitHub remote was fetched and authenticated, but no commit has been pushed, no tag or Release has been created, and production has not been touched.

## Current Resume Point (GitHub Preparation)

Prepare a clean incremental public candidate from `origin/main`, excluding `docs/progress` and `docs/superpowers`, and run the repository secret scanner plus release checks against that exact candidate. Do not push `main`, create `v0.1.3`, publish the Release, or trigger production deployment without explicit user approval.

## Public Candidate Ready

- Local `public-main` was fast-forwarded to remote `main` commit `90a6bfb`, then populated from the sanitized internal archive without rewriting the public history.
- Public candidate commit `7ca2836` (`release: prepare ArtiChat 0.1.3`) is one commit ahead of `origin/main` and contains 157 changed files.
- The public export scanner passed. `docs/progress`, `docs/superpowers`, `deploy-backups`, `.env`, runtime databases, keys, archives, build output, and dependency directories are absent from the committed candidate.
- Exact-candidate verification passed: 150 backend tests, 13 frontend tests, Python compilation, all four release/source guards, Pyodide preparation from a clean checkout, and the Vite production build.
- The first clean Pyodide fetch exceeded the combined 10-minute build wrapper while downloading uncached packages. Continuing with the partial cache completed Pyodide preparation in about 6 minutes, and the isolated Vite build then passed in about 76 seconds without changing tracked source or lock files.
- GitHub still points `main` at `90a6bfb`; only `v0.1.2` exists. No push, `v0.1.3` tag, GitHub Release, GHCR image, Actions release run, or production deployment has been created.

## Current Resume Point (Public Candidate)

Both the internal implementation commit and the clean public candidate are ready. Explicit approval is still required before pushing `public-main` to `origin/main`. Creating and pushing the annotated `v0.1.3` tag is a separate release action because it triggers the GitHub Actions image build, GHCR publication, and GitHub Release; production deployment remains a third, separately approved action.

## Docker Space Cleanup

- Docker initially reported `93.89 GB` of reclaimable BuildKit cache, an unused 7.02 GB dangling ArtiChat image, and an unused 6.39 GB `artichat:main` image.
- All BuildKit cache was removed in two health-neutral passes, reclaiming `93.89 GB`. Future local builds remain functional but will perform a cold dependency build; GitHub Actions uses its separate registry cache.
- Removed unused image IDs `c645390287d2` and `de62e9a68ff2`, reclaiming about another 12.9 GB of unique image data.
- Removed the unmounted, empty `artichat_ollama` volume. The `artichat_data` volume was not changed or detached.
- Post-cleanup Docker usage is 6.659 GB of images, 125 MB of container writable data, 1.089 GB in the active data volume, and 0B of build cache.
- The current `c1a2bd92ac3e` image keeps both `artichat:0.1.3-local` and `artichat:0.1.3-upload-candidate` tags without duplicating layer storage. The running container remains healthy with zero restarts, the same network/restart policy and `artichat_data` mount, version `0.1.3 (Artivis Alpha)`, build hash `local-0.1.3-release-announcement`, three users, nine email templates, one release-announcement row, and migration head `f9a0b1c2d3e4`.

## Docker VHDX Compaction And GitHub 0.1.3 Publication

- After logical Docker cleanup, the custom Docker Desktop data disk at `E:\Docker\DockerData\wsl\disk\docker_data.vhdx` still occupied 106.28 GiB while its ext4 filesystem used about 8.9 GiB.
- ArtiChat, Docker Desktop, and WSL were stopped cleanly. An elevated `Optimize-VHD -Mode Full` reduced the VHDX to 11.54 GiB, releasing about 94.74 GiB of physical E-drive space; normal Docker startup grew it only slightly to 11.57 GiB.
- Pre- and post-compaction SQLite `PRAGMA integrity_check` returned `ok`. The three users, three announcements, one `ann_release_0_1_3` row, nine email templates with nine HTML bodies, and Alembic head `f9a0b1c2d3e4` remained unchanged.
- Local `http://localhost:3000` returned healthy after restart with zero container restarts, preserved `artichat_data`, version `0.1.3 (Artivis Alpha)`, and build hash `local-0.1.3-release-announcement`.
- Public commit `7ca2836196fb1a76458e0b376ba50d0e7fd7a9f2` was fast-forwarded to `origin/main`.
- Annotated tag `v0.1.3` was pushed and triggered GitHub Actions run `29263375237`, which completed successfully in 9m41s across all guards, tests, application build, GHCR build/push, image inspection, and GitHub Release publication.
- Stable Release: `https://github.com/PYBu/ArtiChat/releases/tag/v0.1.3`. Its body exactly matches `docs/releases/0.1.3.md`; it is neither a draft nor a prerelease.
- Public image `ghcr.io/pybu/artichat:0.1.3` and `:latest` share OCI index digest `sha256:d08f1725530f7d3895956c9e750639353c4cbd4dc67c51678cf9554a32f317b8`. Anonymous manifest access returned HTTP 200.
- The linux/amd64 image manifest is `sha256:790be2c5bac7a101f9bb2192500a4c0bada66c3cdbad40f2f093899f596989df`, and its config exposes `WEBUI_BUILD_HASH=7ca2836196fb1a76458e0b376ba50d0e7fd7a9f2`.
- No `artichat-deploy.yml` workflow run was created. Production deployment and production data remain untouched and require separate approval.

## Production 0.1.3 Deployment

- The first administrator-triggered deployment attempt reached the production backend, but GitHub rejected both workflow-dispatch requests with HTTP 403. Cloudflare converted the backend HTTP 502 JSON response into an HTML error page, and the 0.1.2 frontend attempted to parse that HTML as JSON, producing `Unexpected token '<'`.
- Production remained healthy on 0.1.2 throughout the failed attempt. The update state ended at `failed` and inactive, no deploy workflow run existed, and no container restart or data mutation occurred.
- The production fine-grained token was still valid for the `PYBu` identity, repository Actions were enabled, and the deploy workflow was active. Correcting the token's repository `Actions` permission to `Read and write` resolved the dispatch failure.
- After explicit user approval, production token dispatch returned HTTP 204 for operation `retry-0.1.3-20260713T161540Z` and created deploy workflow run `29265688303`.
- The workflow completed successfully in 1m52s. The restricted runner pulled the immutable 0.1.3 image, stopped only ArtiChat, backed up the data directory, recreated the service, and passed health/version verification without rollback.
- Production update state is `completed`, inactive, and error-free. `chat.artivis.cc` reports `0.1.3 (Artivis Alpha)` with build hash `7ca2836196fb1a76458e0b376ba50d0e7fd7a9f2`.
- The running image is `ghcr.io/pybu/artichat:0.1.3` at OCI digest `sha256:d08f1725530f7d3895956c9e750639353c4cbd4dc67c51678cf9554a32f317b8`; ArtiChat is healthy, and SearXNG, Valkey, and 1Panel OpenResty remained running.
- Backup `artichat-data-20260713-161605-retry-0.1.3-20260713T161540Z.tar.gz` is 987,571,473 bytes. Both `gzip -t` and a complete tar listing passed.
- Post-deploy SQLite `PRAGMA integrity_check` returned `ok`, and Alembic reached `f9a0b1c2d3e4`. Users, chats, messages, files, knowledge, subscriptions, ledger entries, usage rows, plans, redemption data, gift-card grants, and all other pre-existing business counts remained unchanged. Expected 0.1.3 migrations added the email tables, configuration rows, and release announcement.
- The root filesystem retained about 24 GiB free after deployment. Local `http://localhost:3000` also remained healthy on 0.1.3 with build hash `local-0.1.3-release-announcement` for continued acceptance testing.

## Production Acceptance And Next Resume Point

- Immediately after deployment, the already-open administrator page still ran the in-memory 0.1.2 frontend while `/api/version` reported the new backend version. This made the UI appear unchanged except for the version number; production HTML, immutable assets, backend routes, and migrations were already on 0.1.3.
- A hard browser refresh loaded the new frontend. The user confirmed the registration management, email, subscription operations, and other 0.1.3 changes are visible and production acceptance is complete.
- Resume future work from the clean `artichat-brand-whitelabel` worktree. Re-read this file, verify `https://chat.artivis.cc` and `http://localhost:3000`, preserve production and local data, and continue following the repository `AGENTS.md` port-3000 acceptance rule. No further 0.1.3 release or deployment action is pending.
