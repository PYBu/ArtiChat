# ArtiChat 0.2.3 Development Progress

Last updated: 2026-08-05 21:47 (Asia/Hong_Kong)

Status: 0.2.3 release preparation is complete in the dedicated `artichat-0.2.3` worktree. The 0.2.2 source worktree remains preserved and dirty; production port 3000 has not been replaced.

## Scope

- Publish the SMTP enable-toggle fix as ArtiChat 0.2.3.
- Keep the first enable action usable when the saved SMTP configuration is empty, while keeping operational SMTP validation fail-closed until host and sender settings are complete.
- Publish the source commit to GitHub `main`, create annotated tag `v0.2.3`, and let the tag-driven release workflow build and publish the GHCR image and GitHub Release.

## Implementation

- Added `smtp_settings_configured` and `smtp_settings_ready` helpers and the public `configured` state in `backend/open_webui/utils/email_delivery.py`.
- Allowed only the first enable request against a completely empty saved SMTP configuration to persist `enabled: true`; later enabled saves still require SMTP host and sender values.
- Updated email challenge, authentication, account-notification, and worker paths to require a ready SMTP configuration before sending.
- Updated the admin Email UI and API types so SMTP fields appear after enabling and registration email controls remain unavailable until SMTP is configured.
- Updated semantic version sources and current product-facing version guards to `0.2.3`.

## Verification

- Backend focused email/admin tests: 21 passed; related challenge/password-reset/notification/delivery tests: 14 passed (35 total).
- Frontend Vitest: 16 files, 37 tests passed.
- Release version, branding, subscriptions, and update-system guards passed for `v0.2.3`.
- `npm ci --force` completed; the full frontend Vitest suite passed with 16 files and 37 tests.
- The complete email/admin/challenge/password-reset/notification/delivery regression set passed with 35 tests.
- `npm run build` passed, including Pyodide package preparation and the Vite production build.
- Python compileall and `git diff --check` passed. Ruff F checks remain clean on the touched files except the five pre-existing `auths.py` baseline findings.

## Release State

- Worktree: `C:\Users\admin\Desktop\Pro\ArtiChat\.worktrees\artichat-0.2.3`
- Branch: `artichat-0.2.3`, based on the verified remote `main` tip before this release.
- Production `3000` remains on 0.1.7 and untouched. The private 3002 candidate is not a production promotion.
- No credentials, tokens, or authenticated request data are stored in this record.

## Handoff

1. Commit the release, push `main`, and create `v0.2.3` after confirming remote `main` has not advanced.
2. Monitor the tag-triggered GitHub Actions run and verify the public release and GHCR image digest.
3. Update this record and the ArtiChat current-state reference with the final commit, workflow run, release URL, and image digest.
