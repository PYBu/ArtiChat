# ArtiChat 0.2.4 Update-System Repair (2026-08-05)

## Scope

- Fix the admin update panel appearing connected while silently treating a missing update repository as "up to date".
- Make disabled checks, missing repository configuration, and GitHub failures visible in the update response.
- Provide the official `PYBu/ArtiChat` repository as the application and production Compose default.
- Make the AWS/1Panel deployment runner derive `ghcr.io/pybu/artichat` when no image repository is configured.
- Bump the application version to 0.2.4 for the repair release.

## Implementation

- `backend/open_webui/env.py` defaults `ARTICHAT_UPDATE_REPOSITORY` to `PYBu/ArtiChat`; `ENABLE_VERSION_UPDATE_CHECK=false` remains the explicit offline opt-out.
- `ArtiChatUpdateService` now carries an enabled flag and returns explicit error text for disabled checks or a missing repository. Deployment rejects disabled checks before dispatch.
- The admin update router avoids constructing a GitHub client while checks are disabled. The legacy `/api/version/updates` response now includes `update_available` and `error` fields.
- `.env.example` and production Compose use the official repository default. `artichat-deploy.sh` derives the image repository from the configured update repository or its official default.
- Added focused update-service/router coverage and a deployment-runner default-repository case. Static update-system guard now protects the defaults and explicit error behavior.

## Verification

- `python -m pytest backend/open_webui/tests/updates/test_update_service.py backend/open_webui/tests/updates/test_update_router.py -q`: 23 passed.
- `python -m pytest backend/open_webui/tests/subscriptions/test_platform_settings.py backend/open_webui/tests/subscriptions/test_upgrade_runtime_compatibility.py -q`: 13 passed, one existing SQLAlchemy deprecation warning.
- `npm ci --force`: completed; npm reported the repository's existing audit/deprecation warnings.
- `npm run test:frontend -- --run`: 16 files / 37 tests passed.
- `npm run test:release-version`, `npm run test:updates`, `npm run test:subscriptions`, and `npm run test:branding`: passed.
- `git diff --check`: passed with expected Windows line-ending warnings.
- `bash deploy/aws-1panel/tests/test-artichat-deploy.sh` could not run in this Windows session because WSL has no `/bin/bash`; the shell harness was not observed failing an assertion.
- `npm run build` was started and stopped after remaining in the existing Pyodide preparation step without output; a clean GitHub runner build remains required.

## Production Boundary

- No production container, data, server environment, SSH runner, GitHub secret, or deployment was changed in this milestone.
- The last live check still showed `https://chat.artivis.cc/api/version` on 0.2.2 while GitHub latest is 0.2.3. The production SSH endpoint was unreachable during diagnosis.
- After 0.2.4 is published, production requires a one-time environment synchronization/restart so the running container receives the repository default and existing deployment token. Do not print or store the token in project records.

## Next Step

Local repair commit: `910153239` (`fix: reconnect ArtiChat update system`). Obtain explicit approval before publishing `v0.2.4`, verify the GitHub Actions release and GHCR image, then arrange the one-time production environment sync before expecting the admin panel to deploy updates.
